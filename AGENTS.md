# Development Completion Rule

For this project, after finishing any development change:

1. Run `npm run lint`.
2. Run `npm run build`.
3. Commit the completed change with a concise message.
4. Push the current branch to GitHub.
5. Verify the GitHub Pages deployment workflow completes successfully.

The site is deployed by the `Deploy to GitHub Pages` GitHub Actions workflow after pushes to `main`.
